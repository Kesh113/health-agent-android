*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# AAR (Samsung SDK — не коммитим, скачивается отдельно)
app/libs/*.aar

# Gradle wrapper (optional — can commit if needed)
# gradle/wrapper/gradle-wrapper.jar
\n# Server-side proxy (не для Android-проекта)\nhealth-proxy.py
